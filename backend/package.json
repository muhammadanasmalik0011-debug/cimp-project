{
  "name": "cims-api",
  "version": "2.0.0",
  "private": true,
  "description": "Professional PERN backend for the City Infrastructure Mapping System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check server.js && node --check db.js && node --check routes/facilities.js && node --check routes/analysis.js && node --check routes/stats.js && node --check routes/zones.js && node --check routes/roads.js && node --check routes/meta.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "express-rate-limit": "^8.0.1",
    "helmet": "^8.1.0",
    "morgan": "^1.10.1",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
